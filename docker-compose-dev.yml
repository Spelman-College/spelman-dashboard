version: "3"
services: 
  react-app: 
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      #  - /app/node_modules
    environment:
      - REACT_APP_NAME=spelman-dev